# 62.题目：查找字符串。　　




sStr1 = 'abcdefg'
sStr2 = 'cde'
print (sStr1.find(sStr2))

'''
find()方法语法：

str.find(str, beg=0, end=len(string))

参数
str -- 指定检索的字符串
beg -- 开始索引，默认为0。
end -- 结束索引，默认为字符串的长度。
返回值

如果包含子字符串返回开始的索引值，否则返回-1。
'''
